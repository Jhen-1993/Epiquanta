# 中介分析：連續 X 的設定

一般中介支援連續或類別 X；中介 M 與結果 Y 目前限定為連續數值。

1. 分析目的選「路徑與機制分析」，分析方式選「一般中介」。
2. 選擇 X、M、Y；依研究問題選擇要調整的干擾因子。
3. X 為連續時，預設「每增加指定單位（預設 1）」。例如 Age 每增加 1 歲；輸入 10 則報告每增加 10 歲的效果。此模式不需填 a0、a1。
4. 若需明確比較兩個數值，選「指定兩個數值比較（a0 → a1）」，例如 50 → 60。兩者須不同，且位於模型完整個案的觀察範圍內。X 仍保持連續，所有符合分析條件的個案都會使用，並非只取這兩個年齡或切成兩組。
5. X 為類別時，選擇參考組和比較組；多類別 X 僅保留所選兩組。切換回類別 X 時，舊的連續 X 單位設定不會影響組間對比。
6. 按「送出並執行分析」。模型結果表註、圖形副標題、效果設定表與計算紀錄都會標明這次 X 的效果單位。

## 計算與解讀

採用線性路徑模型 M ~ aX + C、Y ~ c′X + bM + C，其中 C 只包含使用者選取的干擾因子。直接效果為 c′ × ΔX，間接效果為 a × b × ΔX，總效果為兩者相加。因為目前的一般中介是線性且無 X×M 交互作用，連續 X 的每單位效果不需任意選擇參考值。

估計值與信賴區間使用相同 ΔX 縮放；反向對比保留正確的區間上下界。p 值仍來自主模型推論，不因單位換算而改變。Bootstrap 使用相同的效果單位。每單位斜率不表示可不受限制地外推至資料範圍外。

因果中介的效果可能依賴基準暴露值、交互作用及效果尺度，因此仍需指定 a0、a1；沒有套用一般中介的每單位捷徑。因果推論亦需研究設計及識別假設支持，不能只依中介效果的顯著性判斷。

方法參考：[lavaan 中介分析](https://lavaan.ugent.be/tutorial/mediation.html)。
