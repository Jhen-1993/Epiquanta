# 量表品質與效度

先匯入資料，指定研究設計與獨立個案結構，再選「量表品質與效度」。信度、EFA、CFA 是不同目的的工具，不代表每份資料都必須依序執行。

## 信度

1. 選「信度分析：Cronbach α」，使用構面卡片選題。
2. 按「＋新增構面」，填名稱，在該卡片搜尋、多選題項。可全選或清除。
3. 整體量表與分量表可以共用題項，名稱需不同。各題組採自己的完整作答樣本。
4. 若有反向題，在左側選題後才會出現理論上下界。系統不以觀察極值猜量尺。不同上下界需分次分析。
5. 每組至少兩題。兩題列 Pearson 題間 r、Spearman–Brown，保留原始 α；三題以上另列 CITC 與刪題 α。信度係數依實際可用的數值計分計算，並非依示意圖填值。
6. 固定 5,000 次受試者層級非參數 Bootstrap 百分位 CI，記錄每組種子及有效次數；任何係數有效比例低於 95% 停止分析。原始 α 使用原計分共變異數，標準化 α 使用 Pearson 相關矩陣。

進階文字題組設定仍可使用。切換至 CFA 或回到信度卡片時，保留該工作階段的卡片；重新匯入資料會重新建立配置。

## 驗證性因素分析 CFA

1. 選「驗證性因素分析 CFA」，使用構面卡片。填構面代碼與名稱，例如 F1 工作環境，逐構面選題。
2. 明確選擇估計範圍：
   - **聯合 CFA**：所有構面一起配適，使用所有模型題項共同的完整個案；估計整體配適度、構面相關與區辨效度。
   - **分構面 CFA**：每個題組獨立配適單因素模型，使用該題組自己的完整作答樣本；逐組檢查負荷、CR／AVE、配適與失敗原因。分構面結果不能替代聯合模型驗證。
3. 聯合 CFA 可允許構面相關（預設），或依理論固定為 0。**分開估計不等於構面不相關**；固定為 0 的相關不作區辨效度通過的證據。
4. 估計法選「依已確認題型自動選擇」時，連續題採 MLR，含已確認順序題採 WLSMV。分構面模式逐組判斷，並記錄每組實際方法。未指定順序的名目題需先確認計分／順序，不猜設高低。
5. 「驗證樣本」表示研究資料來源，與分構面／聯合是不同設定。獨立驗證樣本需由研究流程另行取得及匯入；選單不會自動切分資料。
6. 卡片基本模式每構面至少三題；兩題識別限制、跨負荷或高階因素請用進階 lavaan 語法。進階語法依整份指定模型配適，分構面批次只在卡片模式提供。

## 結果與匯出

CFA 優先顯示估計範圍、標準化負荷量及 95% CI、CR／AVE、適用的區辨效度、配適度與估計檢查，測量圖放在後面。每張表可下載 Word／Excel，每張圖可下載 PNG／PDF；完整原始參數在「完整分析紀錄與原始係數」。

- 負荷量 CI 取自 `lavaan::standardizedSolution(type="std.all")`，為 delta method／對稱常態近似，沒有誤用未標準化係數的 CI。
- CR 使用 `semTools::compRelSEM(obs.var=TRUE, tau.eq=FALSE, ord.scale=TRUE)`；順序題對應觀察反應尺度，AVE 對應潛在反應尺度。表格標明尺度，不能只用門檻判定效度。
- 聯合 CFA 的 HTMT2 是幾何平均版本，依順序或連續題採用適用相關；不冒稱 Pearson HTMT 或 Spearman 敏感度分析。HTMT2 CI 只有在真正啟用 Bootstrap、有效次數足夠時提供。
- 分構面 CFA 不合併生成整體配適度、不產生跨構面相關或區辨效度；失敗構面保留原因，不用其他構面的數字填補。
- 單因素三題通常剛好識別（df=0），整體配適度無法證明模型正確。未自動刪題，亦未執行範例圖片的 15／25 點數值積分敏感度檢查。

## 與 SEM 的區隔

CFA 問「題目是否支持預定的測量構面」；「路徑與機制分析 → SEM」進一步設定終點 Y、預測／中介構面及方向性路徑。CFA 是 SEM 架構中的測量模型，圖形符號相似，但分析目的與主要輸出不同。

方法文件：[lavaan CFA](https://lavaan.ugent.be/tutorial/cfa.html)、[順序題與 WLSMV](https://lavaan.ugent.be/tutorial/cat.html)、[standardizedSolution](https://rdrr.io/cran/lavaan/man/standardizedSolution.html)、[compRelSEM](https://rdrr.io/cran/semTools/man/compRelSEM.html)、[HTMT2](https://rdrr.io/cran/semTools/man/htmt.html)。
