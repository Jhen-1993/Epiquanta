# SEM 與 EFA 構面操作

1. 匯入資料並指定研究設計、資料結構。SEM：分析目的選「路徑與機制分析」，分析方式選「結構方程式（SEM）」。
2. 在「構面與路徑」先完成「01 終點結果 Y」。觀察結果選單一欄位，確認自動辨識的型態。二元結果必須選擇事件組，另一類自動為非事件組；順序結果確認由低至高的級別順序。若 Y 是潛在結果，直接在該區選擇其測量題項。
3. 在「02 其他構面與觀察變項」按「＋新增構面／變項」，填代碼與名稱，例如 F1 工作環境、F2 疲勞、F3 職能認知。Y 已獨立存在，不需再新增 Y 節點。
4. 按「＋新增路徑」，逐條指定起點、關係及終點。F1 → F2 → F3 → Y 需新增三條單向路徑；直接效果另行加入。簡易設定的 Y 是終點，至少需要一條通往 Y 的單向路徑。
5. 在各潛在構面卡片搜尋、多選測量題項；Y 的題項在第一區選。簡易設定要求每個潛在構面至少三題，且不重複配置；兩題識別限制、跨負荷、循環模型或多終點請改用進階 lavaan 語法。
6. 已確認的順序題與 Y 設定自動帶入。選擇 WLSMV，可展開查看本次實際題項及級別順序。「補充順序題設定（選填）」只列出本模型尚需設定的欄位；無關或舊的勾選不會阻止分析，也不會被加入模型。完全連續的模型可依假設選 ML／MLR。
7. 確認估計設定後按「送出並執行分析」。結果先顯示 Y 與事件定義、模型配適度，再顯示 SEM 圖。每張表可下載 Word／Excel，每張圖可下載 PNG／PDF。

## SEM 圖形與數值

- 橢圓：潛在構面；方框：觀察題項或結果。
- 單向箭頭：測量負荷或結構路徑；雙向箭頭：模型估計的共變異。
- e 圓圈：測量誤差；d 圓圈：內生變項的擾動。旁列實際標準化殘差變異數。
- 箭頭係數使用 lavaan 的 Std.all；結構路徑及共變異的星號根據原始未標準化參數之 Wald p 值。固定測量負荷沒有估計 p 值，測量箭頭不標星號。
- 二元／順序結果使用潛在反應尺度。二元 WLSMV 的路徑係數屬 probit 模型，不可解讀為 Logistic OR。事件方向與順序記錄在結果表。
- 圖中省略外生變項的自變異。配適度另列，記錄 robust／scaled 指標的實際來源；圖形與配適度不會單獨確立因果關係。
- 較大的圖可在圖框內水平捲動；完整向量圖可下載 PDF。統計套件與繪圖套件的版本分開列出。

## EFA

EFA：分析目的選「量表品質與效度」，分析方式選 EFA。在「構面與路徑」按「＋新增構面」，直接選各題組的欄位。每張卡片獨立執行 EFA，因素數仍由平行分析或指定值決定。若要探索整份量表，將相關題項放在同一張卡片。

Logo：頁首與分頁名稱為 Epiquanta；SVG 使用短暫進場及滑鼠移入動畫，遵循 prefers-reduced-motion。

方法依據：
- https://lavaan.ugent.be/tutorial/syntax1.html
- https://lavaan.ugent.be/tutorial/sem.html
- https://lavaan.ugent.be/tutorial/inspect.html
- https://lavaan.ugent.be/tutorial/cat.html

本次使用固定亂數種子的合成資料驗證，包括終點型態、事件方向、舊勾選排除、動態構面狀態、圖形參數與匯出。檢查程式位於 tests，並非使用者研究資料的分析報告。
